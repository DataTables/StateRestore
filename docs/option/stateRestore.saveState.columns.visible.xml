<?xml version="1.0" encoding="UTF-8" ?>
<dt-option library="StateRestore">
	<name>stateRestore.saveState.columns.visible</name>
	<summary>Enable/Disable saving the column visibility</summary>
	<since>0.0.1</since>

	<type type="boolean">
		<description>
            True if column visibilities are to be saved, false if not.
        </description>
	</type>

	<default value="true">
        The default value for the `-init stateRestore.saveState.columns.visible` option is `true` meaning that as standard the column visibilities are saved.
	</default>
	<description>
		<![CDATA[
		This option allows for the saving of the visibility of the columns to be enabled or disabled.

		As default the value is true, meaning that the column visibilities will be saved. If this option is set to false, then column visibilites will not be saved.

		Take a look at [this example]([../extensions/StateRestore/examples/customisation/noColumnVisibility.html) to see this in action.

        ]]>
	</description>

<example title="Disable column visibility saving"><![CDATA[
 $('#example').DataTable({
    dom: 'Blfrtip',
    buttons:[
        'createStateRestore',
        {
            extend: 'savedStates',
            config: {
                columns:{
                    visible: false
                }
            }
        },
        'colvis'
    ]
});
]]></example>

</dt-option>