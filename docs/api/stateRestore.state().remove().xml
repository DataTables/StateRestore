<?xml version="1.0" encoding="UTF-8" ?>
<dt-api library="StateRestore">
	<name>stateRestore.state().remove()</name>
	<summary>Delete a state</summary>
	<since>1.0.0</since>

	<type type="function">
		<signature>stateRestore.state().remove()</signature>
		<description>Deletes the selected state.</description>
        <parameter type="boolean" name="skipModal" default="false">
			Boolean value indicating if the confirmation modal should be skipped or not. The default is `false` meaning that the modal will be shown. If set to `true` the state will be immediately deleted.
		</parameter>
		<returns type="DataTable.Api">Returns a DataTables API instance for chaining.</returns>
	</type>

	<description>
	<![CDATA[

This method provides the ability to remove a state that is managed by the StateRestore extension.

`-api stateRestore.state().remove()` cam take a single argument, `skipModal` that allows the confirmation modal to be bypassed. The default for `skipModal` is `false`, meaning that the modal will be shown. It removes the state retrieved by the previous call to `-api stateRestore.state()`.

Note that predefined states (`-init stateRestore.preDefined`) cannot be removed, nor can states which have been shared by other users.

	]]>
	</description>

	<example title="Remove a state, showing the confirmation modal"><![CDATA[

table.stateRestore.state('{id}').remove();

]]></example>

	<example title="Remove a state, skipping the confirmation modal"><![CDATA[

table.stateRestore.state('{id}').remove(true);

]]></example>

	<related type="api">stateRestore.state()</related>
	<related type="api">stateRestore.states().remove()</related>
</dt-api>