<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" table-class="display nowrap" order="1">

<css lib="datatables buttons staterestore"/>

<js lib="jquery datatables staterestore buttons">
<![CDATA[
$(document).ready(function() {
    $('#example').DataTable({
        dom: 'Blfrtip',
        buttons: ['savedStatesCreate']
    });
});
]]>
</js>

<title lib="stateRestore">Alternative UI</title>

<info><![CDATA[

This example shows how StateRestore can be initialised using a single button.

Here the `-button savedStatesCreate` button is used, instead of both the `-button savedStates` and `-button createState` buttons. This button instead adds the `-button createState` to the collection that is normally displayed when the `-button savedStates` button is pressed.
]]></info>

</dt-example>