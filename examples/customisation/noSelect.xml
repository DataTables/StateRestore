<?xml version="1.0" encoding="UTF-8" ?>
<dt-example table-type="html" table-class="display nowrap" order="12">

<css lib="datatables buttons staterestore select"/>

<js lib="jquery datatables staterestore buttons select">
<![CDATA[
$(document).ready(function() {
    $('#example').DataTable({
        dom: 'BPlfrtip',
        buttons:[
            'createState',
            {
                extend: 'savedStates',
                config: {
                    saveState: {
                        select: false
                    }
                }
            }
        ]
    });
});
]]>
</js>

<title lib="stateRestore">Don't save Selections</title>

<info><![CDATA[
This example shows how to use the `-init stateRestore.select` initialisation option to disable the saving of the table's current `-init select` search criteria by setting the value of the initialisation option to `false`. The default value is `true` meaning that the selections will be saved.
]]></info>

</dt-example>